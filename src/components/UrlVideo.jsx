import React from 'react';

const UrlVideo = ({url}) => {
    return (
        <div>
            <span className='urlVideo'>{url}</span>
        </div>
    );
};

export default UrlVideo;

// L'url est cacher par la meme couleur du bg 